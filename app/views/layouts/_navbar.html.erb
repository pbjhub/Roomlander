<nav class="navbar navbar-inverse navbar-static-top" id="nav1">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= landing_search_path %>">Search</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1" >
      
      <form class="navbar-form navbar-left">
      <% if !@curacao && !@pot %>
        <div class="form-group" >
          <input type="text" class="form-control" placeholder="Where do you want to live?" name="buscador">
        </div>
      <% end %>

      
      </form>
      
      <ul class="nav navbar-nav navbar-right">
      
      <% if user_signed_in? %>
       <% if !@pot %>
       <li>
       <a href="<%= rooms_path %>"> Offer a room</a>
       </li>
       <% end %>
       <% if !@eljefe %>
       <li>
       <a href="<%= conversations_path %>"> Inbox </a>
       </li>
        <% end %>
       <% if !@curacao %>
       <li>
       <a href="<%= edit_user_registration_path %>"> Account</a>
       </li>
       <% end %>
       <% end %>

      
       
       

      <% if user_signed_in? %>
      <li> <a href="<%= destroy_user_session_path %>" data-method='delete'>
       Log out
      </a></li>
      <% else %>
      <li><a href="<%= new_user_session_path %>"> Login </a></li>
      <li><a href="<%= new_user_registration_path %>"> Sign Up </a></li>
      <% end %>
        
       
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>